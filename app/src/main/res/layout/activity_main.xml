<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/CFFF"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/tbMain_Toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_shape_maintoolbar"
        android:theme="@style/AppTheme"></android.support.v7.widget.Toolbar>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_shape_listtitle"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnMain_Refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="刷新列表"></Button>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"></TextView>

        <Button
            android:id="@+id/btnMain_New"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="新增采集数据"></Button>
    </LinearLayout>

    <TextView
        android:id="@+id/lblMain_NoData"
        android:layout_width="match_parent"
        android:layout_height="57dp"
        android:background="@color/CFFF"
        android:gravity="center"
        android:text="没有采集数据记录，您可以新增采集数据" />

    <ListView
        android:id="@+id/lvMain_List"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"></ListView>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="57dp"
        android:background="@drawable/bg_shape_viewfooter"
        android:gravity="center"
        android:text="@string/app_version"
        android:textColor="@color/White"
        android:textSize="20dp" />
</LinearLayout>